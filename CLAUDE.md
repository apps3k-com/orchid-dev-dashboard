# CLAUDE.md — <REPO>

The binding working rules live in **`AGENTS.md`** and apply equally to Claude Code
and Codex. This file imports them so there is a single source of truth (no drift).

@AGENTS.md

> Claude-specific: mandatory hooks live in `.claude/hooks/` (Codex: `.codex/hooks/`).
> Detail is read **on demand** from `docs/agents/*`, never duplicated here.
